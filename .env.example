# Slack Bot Token (starts with xoxb-)
# Found in: OAuth & Permissions → Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack App Token (starts with xapp-) - Only needed for Socket Mode
# Found in: Basic Information → App-Level Tokens (needs connections:write scope)
SLACK_APP_TOKEN=xapp-your-app-token

# Slack Signing Secret
# Found in: Basic Information → App Credentials → Signing Secret
SLACK_SIGNING_SECRET=your-signing-secret

# Set to 'true' for HTTP mode (production/serverless)
# Leave unset or 'false' for Socket Mode (local development)
HTTP_MODE=false

# Port for HTTP mode (default: 3000)
PORT=3000
